lambda: http

http:
  address: ":8080"
  mode:
    debug: true
    cors: true

dynamic:
  environment:
    toolchain:
      os: windows10.0.26200.0
      arch: amd64v1
      compiler: node26.2.0
      variant: full
    warehouse:
      local: /tmp/app-http/warehouse
      remote: s3://lambda-node-app-http
  package:
    namespace: app
    defaultVersion: v1
    preload:
      - package: apifull
        version: v1
      - package: wapifull
        version: v1
